const SectionHeader = ({ title }) => {
  return (
    <div className="w-full flex p-3">
      <h2 className="text-xl font-semibold text-secondary-100">{title}</h2>
    </div>
  );
};

export default SectionHeader;
